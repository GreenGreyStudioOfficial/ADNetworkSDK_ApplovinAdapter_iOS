import XCTest
@testable import ADNetworkSDK_ApplovinAdapter

final class ADNetworkSDK_ApplovinAdapterTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(ADNetworkSDK_ApplovinAdapter().text, "Hello, World!")
    }
}
